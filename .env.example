# ==========================================
# Core OpenCode Web Configuration
# ==========================================
# These are the three canonical environment variables for OpenCode Web.
# Use these for all new deployments and configurations.

# OpenCode Server URL (default: http://localhost:4096)
# Full URL to the OpenCode server including scheme, host, and port
OPENCODE_SERVER_URL=http://localhost:4096

# Web UI Host (default: localhost)
# Interface to bind the web server to. Use 0.0.0.0 to listen on all interfaces
OPENCODE_WEB_HOST=localhost

# Web UI Port (default: 3000)
# Port for the web UI server
OPENCODE_WEB_PORT=3000

# ==========================================
# Legacy Variables (Deprecated)
# ==========================================
# These variables are maintained for backward compatibility but will be removed
# in a future version. Please migrate to the canonical variables above.
#
# PORT=3000                           # Use OPENCODE_WEB_PORT instead
# HOST=localhost                      # Use OPENCODE_WEB_HOST instead
# VITE_OPENCODE_SERVER_URL=...        # Use OPENCODE_SERVER_URL instead
# OPENCODE_SERVER_PORT=4096           # Configure via OPENCODE_SERVER_URL
# OPENCODE_SERVER_HOSTNAME=localhost  # Configure via OPENCODE_SERVER_URL

# ==========================================
# Optional Configuration
# ==========================================
# Additional optional settings for specific use cases

# PWA Assets URL (optional)
# URL prefix for PWA assets when using a CDN
# VITE_PWA_ASSETS_URL=https://assets.yourdomain.com

# Allowed Hosts (optional)
# Comma-separated list of allowed hostnames for the dev server
# VITE_ALLOWED_HOSTS=localhost,yourdomain.com

# Base Path (optional)
# Base path for reverse proxy deployments
# VITE_BASE_PATH=/opencode

# CORS Origins (optional)
# Comma-separated list of allowed CORS origins
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# CSS Transformer (optional, default: postcss)
# Choose between 'postcss' or 'lightningcss'
# OPENCODE_WEB_CSS_TRANSFORMER=postcss
